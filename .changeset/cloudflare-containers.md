---
"@opennextjs/cloudflare": minor
"@opennextjs/core": patch
---

Add Cloudflare Containers support for the default Next.js server, including Worker middleware forwarding, runtime environment propagation, request protocol and cancellation handling, and container E2E coverage.

Generate Node.js container images from a glibc base and rebuild native dependencies for the target Linux platform.
